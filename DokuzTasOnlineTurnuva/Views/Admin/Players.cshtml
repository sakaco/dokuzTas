@model IEnumerable<DokuzTasOnlineTurnuva.Models.ApplicationUser>

@{
    ViewData["Title"] = "Oyuncu Yönetimi";
}

<div class="min-h-screen p-4 md:p-8">
    <div class="max-w-7xl mx-auto">
        <div class="bg-white rounded-3xl shadow-2xl p-6 md:p-12 mb-8">
            <h1 class="text-3xl md:text-4xl font-bold text-center text-gray-800 mb-8">
                👥 Oyuncu Yönetimi
            </h1>
            
            <div class="mb-6">
                <button onclick="document.getElementById('createModal').classList.remove('hidden')" class="bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-6 rounded-lg">
                    ➕ Yeni Oyuncu
                </button>
            </div>
            
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead>
                        <tr class="border-b-2 border-gray-200">
                            <th class="p-3 text-left">Kullanıcı Adı</th>
                            <th class="p-3 text-center">Puan</th>
                            <th class="p-3 text-center">Averaj</th>
                            <th class="p-3 text-center">Maçlar</th>
                            <th class="p-3 text-center">Durum</th>
                            <th class="p-3 text-center">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var user in Model)
                        {
                            <tr class="border-b border-gray-100 hover:bg-gray-50">
                                <td class="p-3 font-semibold">
                                    <a asp-action="PlayerDetail" asp-route-id="@user.Id" class="text-blue-600 hover:underline">
                                        @user.UserName
                                    </a>
                                </td>
                                <td class="p-3 text-center font-bold text-blue-600">@user.Points</td>
                                <td class="p-3 text-center font-bold text-green-600">@user.Averaj</td>
                                <td class="p-3 text-center">@user.TotalMatches</td>
                                <td class="p-3 text-center">
                                    @if (user.IsBlacklisted)
                                    {
                                        <span class="bg-red-100 text-red-800 px-2 py-1 rounded">🚫 Yasaklı</span>
                                    }
                                    else
                                    {
                                        <span class="bg-green-100 text-green-800 px-2 py-1 rounded">✓ Aktif</span>
                                    }
                                </td>
                                <td class="p-3 text-center">
                                    <form asp-action="ToggleBlacklist" asp-route-id="@user.Id" method="post" class="inline">
                                        <button type="submit" class="bg-yellow-500 hover:bg-yellow-600 text-white px-3 py-1 rounded mr-1">
                                            @(user.IsBlacklisted ? "Aktifleştir" : "Yasakla")
                                        </button>
                                    </form>
                                    <form asp-action="ResetPlayer" asp-route-id="@user.Id" method="post" class="inline">
                                        <button type="submit" onclick="return confirm('Emin misiniz?')" class="bg-orange-500 hover:bg-orange-600 text-white px-3 py-1 rounded mr-1">
                                            Sıfırla
                                        </button>
                                    </form>
                                    <form asp-action="DeletePlayer" asp-route-id="@user.Id" method="post" class="inline">
                                        <button type="submit" onclick="return confirm('Emin misiniz?')" class="bg-red-500 hover:bg-red-600 text-white px-3 py-1 rounded">
                                            Sil
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div id="createModal" class="hidden fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50">
    <div class="bg-white rounded-2xl p-8 max-w-md w-full mx-4">
        <h2 class="text-2xl font-bold mb-4">Yeni Oyuncu Oluştur</h2>
        <form asp-action="CreatePlayer" method="post">
            <div class="mb-4">
                <label class="block text-gray-700 font-bold mb-2">Kullanıcı Adı</label>
                <input name="username" required class="w-full px-4 py-2 border rounded" />
            </div>
            <div class="mb-4">
                <label class="block text-gray-700 font-bold mb-2">Email</label>
                <input name="email" type="email" required class="w-full px-4 py-2 border rounded" />
            </div>
            <div class="mb-4">
                <label class="block text-gray-700 font-bold mb-2">Şifre</label>
                <input name="password" type="password" required class="w-full px-4 py-2 border rounded" />
            </div>
            <div class="flex gap-2">
                <button type="submit" class="flex-1 bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded">
                    Oluştur
                </button>
                <button type="button" onclick="document.getElementById('createModal').classList.add('hidden')" class="flex-1 bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-4 rounded">
                    İptal
                </button>
            </div>
        </form>
    </div>
</div>
